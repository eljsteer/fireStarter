
const Home = () => {
  // const { loading, data } = useQuery(QUERY_PROJECTS, {
  //   fetchPolicy: "no-cache"
  // });

    return (
      <p>This is the Home Page</p>
    );
  };

export default Home;
